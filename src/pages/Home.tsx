import { Center } from '@/components/lib/Center';
import { FancyText } from '@/components/lib/FancyText';

export const Home = () => {
  return (
    <Center>
      <FancyText style={{ fontSize: 32 }}>Главная</FancyText>
    </Center>
  );
};
